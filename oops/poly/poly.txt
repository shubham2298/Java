1.What is polymorphism (Runtime and Compile time)?
    same message is pass to different object and object respond to the same message in different ways is called as polymorphism.

2. Why polymorphism is required/useful in Java?
    1.resuability of code.
        if we perform addition for 1 parameter,2 parameter,3 parameter programer need to write 3 different method but use 
        of polymorphism we need write single method and pass different parameter.

3.Is Inheritance must for Polymorphism in java? Explain.
    their is no need Inheritance must in polymorphism because function overloading perform in same scope means in same class.

